@model Resort_Hub.ViewModels.Auth.RegisterVM

@{
    ViewData["Title"] = "Register";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Account – Resort Hub</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=DM+Sans:wght@400;500&display=swap" rel="stylesheet" />
   <link href="~/css/Register.css" rel="stylesheet" />
</head>
<body>

    <div class="auth-card">
        <!-- Left: image -->
    <div class="panel-image">
        <div class="brand">Resort<span>Hub</span>.</div>
    </div>

    <!-- Right: form -->
    <div class="panel-form">

        <div class="form-inner">
                <a asp-action="Index" asp-controller="Home"
                       style="display:inline-flex;align-items:center;gap:6px;font-size:0.85rem;
                    color:#4f8ef7;text-decoration:none;margin-bottom:1.25rem;">
                    <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" fill="none"
                         viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z" />
                        <polyline points="9 22 9 12 15 12 15 22" />
                    </svg>
                    Back to Home
                </a>

            <h1>Create Account</h1>

            <form asp-action="Register" asp-controller="Auth" method="post" novalidate>
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="name-row">
                    <div class="field-group">
                        <label asp-for="FirstName">First Name</label>
                        <input asp-for="FirstName" placeholder="John" />
                        <span asp-validation-for="FirstName" class="text-danger"></span>
                    </div>
                    <div class="field-group">
                        <label asp-for="LastName">Last Name</label>
                        <input asp-for="LastName" placeholder="Doe" />
                        <span asp-validation-for="LastName" class="text-danger"></span>
                    </div>
                </div>

                <div class="field-group">
                    <label asp-for="Email">E-mail</label>
                    <input asp-for="Email" placeholder="name@gmail.com" />
                    <span asp-validation-for="Email" class="text-danger"></span>
                </div>

                <div class="field-group">
                    <label asp-for="Password">Password</label>
                    <div class="pass-wrap">
                        <input asp-for="Password" type="password" placeholder="6+ characters" id="pwd" />
                        <button type="button" class="pass-toggle" onclick="togglePass('pwd', this)">
                            <svg id="eye-pwd" xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none"
                                 viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8">
                                <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z" />
                                <circle cx="12" cy="12" r="3" />
                            </svg>
                        </button>
                    </div>
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>

                <div class="field-group">
                    <label asp-for="ConfirmPassword">Confirm Password</label>
                    <div class="pass-wrap">
                        <input asp-for="ConfirmPassword" type="password" placeholder="Repeat password" id="cpwd" />
                        <button type="button" class="pass-toggle" onclick="togglePass('cpwd', this)">
                            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none"
                                 viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8">
                                <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z" />
                                <circle cx="12" cy="12" r="3" />
                            </svg>
                        </button>
                    </div>
                    <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
                </div>

                <button type="submit" class="btn-register">Register</button>
            </form>

            <p class="login-link">
                Already have an account? <a asp-action="Login" asp-controller="Auth">Login</a>
            </p>
        </div>
    </div>
    </div>

    <partial name="_ErrorPopUp" />
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/lib/jquery/dist/jquery.js"></script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/dist/jquery.validate.unobtrusive.min.js"></script>

    <script>
            function togglePass(id, btn) {
                const input = document.getElementById(id);
                input.type = input.type === 'password' ? 'text' : 'password';
                btn.style.color = input.type === 'text' ? '#4f8ef7' : '#aaa';
            }
    </script>

</body>
</html>